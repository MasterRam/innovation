
import 'mocha';
import { testFunc } from '../../lib/RND/function.rnd';

describe('Function ', () => {
    it('Should be a valid object', () => {
        // console.log('asdadsads', JSON.stringify(testFunc));

        // testFunc[''].arguments.forEach(element => {
        // });
    });

});
